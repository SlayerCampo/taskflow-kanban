# ============================================================
# .env.example — Plantilla de variables de entorno
# Copia este archivo como ".env" (sin .example) y llénalo con
# tus datos LOCALES. El archivo ".env" NUNCA se sube a GitHub.
# ============================================================

PORT=5000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_contraseña_local
DB_NAME=kanban_db

JWT_SECRET=escribe_aqui_una_clave_secreta_larga
